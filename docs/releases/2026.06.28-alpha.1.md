# RetOS 2026.06.28-alpha.1

Status: Release candidate draft.

Current draft evidence commit: `94e48fa63360a4bf706994fc0b055613dd503143` validated locally and in CI.

Latest local development evidence commit: `9a1f241edcac5e6de4b5c53affc2c1ce58c335cb`.

The draft evidence commit is the code revision covered by the recorded local gates, CI
run, and uploaded audit artifacts. Documentation-only commits may follow it while the
candidate stays in draft, but the immutable release tag commit must be reconciled back
to this note before promotion.

Local-only implementation commits after the draft evidence commit are tracked here so
reviewers do not confuse checkout state with the last CI-published evidence snapshot.
The latest local development evidence commit includes provider runtime policy, runtime
switch planning, branch-coverage ratchet evidence, CI handoff artifact plumbing,
release-note evidence guards, offline CI workflow contract checks, offline audit export
verification, README onboarding usability checks, machine-checked human visual review
evidence, local preflight doctor checks, compact operational UI sections, responsive
Overview sections, compact query workspace sections, organized document Library and Sources sections,
explicit upload/text ingestion context panels,
eval scope context cards,
provider runtime context cards,
audit jobs context cards,
admin users context cards,
query run context cards,
live progress context cards,
audit events context cards,
audit progress context cards,
eval results and history context cards,
document library and source context cards,
compact two-column document Library layout,
ignore hygiene guard tests,
release workflow guard tests,
Docker topology guard tests,
CI workflow guard tests,
published release evidence guard tests,
image metadata guard tests,
image size guard tests,
backend runtime image guard tests,
security policy guard tests,
release notes guard tests,
versioned release notes guard tests,
operations runbook guard tests,
branding assets guard tests,
dependency audit guard tests,
CI status guard tests,
production preflight guard tests,
audit pack guard tests,
compact workspace screen tests,
document UI evidence traceability guard,
auditor evidence matrix guard tests,
README usability guard tests,
production readiness test-count guard,
expired-session recovery across protected
console actions, offline calibration evidence gating, and the idempotent Docker demo
seed for first-run product validation through Makefile, admin API, and React UI entry
points, the backup/restore drill evidence gate, the target security review evidence
gate, and the calibration scope decision evidence gate, with real HTTP smoke and
Playwright coverage for that first-run flow, auditable domain detail editing/archive/restore,
auditable source detail editing/removal, compact Library domain sections with hover/focus
tooltips, and the branch coverage ratchet raised to 90.65%. Treat the CI artifact rows below as evidence
for the draft evidence commit until `make ci-status-check` passes for the release tag
commit under review.

Before tagging or promoting this candidate, the exact immutable release tag commit must
be reconciled with this note and verified with `make ci-status-check`.

Images:

- `ghcr.io/<owner>/retos-backend:2026.06.28-alpha.1`
- `ghcr.io/<owner>/retos-web:2026.06.28-alpha.1`

## Highlights

- Shared backend image for API, worker, and migrations through the `backend-runtime`
  Docker target.
- React operational console for document management, ingestion, indexing, jobs, evals,
  audit events, grounded query results, compact task sections, bounded long panels, and
  hover/focus tooltips for dense operational controls.
- FastAPI backend with persisted domains, sources, documents, versions, artifacts,
  segments, jobs, progress events, and audit journals.
- RabbitMQ-backed Celery worker flow for ingestion, indexing, OCR, and eval jobs.
- Local-first provider posture with Ollama `gemma4` as the default profile and paid
  providers disabled by default.
- Deep Agents harness integration path with deterministic fallback, controlled corpus
  tools, citations, neighboring context, evidence route, evidence audit, contradiction
  audit, bounded multi-hop subquery execution, multi-hop audit, budget accounting, and
  persisted query journals.
- Deterministic smoke and expanded agent multi-hop evals plus opt-in SQuAD, HotpotQA,
  HotpotQA-agent, Natural Questions, NQ-Open adapter, and OCR benchmark paths, with
  React controls for local dataset suites.
- `hotpotqa-agent` calibration maps local HotpotQA supporting facts into deterministic
  agent audit cases through CLI, admin API, rerun, and React controls without provider
  calls.
- Domain-owned dataset eval runs for auditable calibration history, filtered trends,
  scoped reruns, mixed-scope comparison guards, and React `Eval scope` controls.
- Docker release guardrails for topology, backend runtime image ID, OCI metadata, image
  size budgets, release workflow publishing, release notes, and operations documentation.

## Migration Notes

- Apply Alembic migrations with the shared backend image before starting API and worker:

```bash
RETOS_IMAGE_TAG=2026.06.28-alpha.1 docker compose run --rm migrate migrate
```

- This pre-alpha candidate expects an empty or development RetOS database. Backup before
  upgrading any existing environment.
- Search indexes are rebuildable projections. Rebuild indexes after restore or when
  moving storage volumes between machines.

## Compatibility

- Python: 3.14
- Node: 24
- Postgres: 18-bookworm
- RabbitMQ: 4-management
- Ollama: local `gemma4` profile
- Browser: Chromium-family browser for the validated Playwright smoke path

## Security

- License: MIT.
- Containers run as non-root users.
- Development secrets in `.env.example` are placeholders and must be replaced outside
  local development.
- Paid LLM calls remain disabled unless `RETOS_ALLOW_PAID_LLM=true` is set explicitly
  with provider credentials.
- Release images include OCI source/license/revision labels.
- Python and Node dependency advisories are checked with `make dependency-audit`; the
  current runtime pins include patched `orjson` and `Pillow` versions.
- GHCR publishing workflow requests SBOM/provenance attestations, creates Cosign keyless
  signatures, and runs Cosign signature verification for published image digests.

## Validation Evidence

Baseline validation for the draft candidate:

| Gate | Evidence |
| --- | --- |
| Local acceptance | `make local-acceptance` passed across backend quality, API/browser integration, frontend build, visual audit, Docker config, auditor handoff, and Docker stack smoke. |
| Backend format/lint/type/tests | `make check` passed with 729 tests. |
| Backend coverage | 95.43% total coverage, meeting the 90% gate; branch-only coverage is 90.78% with a 90.65% ratchet above the 90% target. |
| Dependency audit | `make dependency-audit` passed: `pip-audit` reported no known Python runtime vulnerabilities and `npm audit --audit-level=high` reported 0 vulnerabilities. |
| Eval smoke | `make check` ran `run_eval_smoke.py` and reported PASS. |
| Release readiness | `make release-check` passed. |
| Release workflow contract | `make release-workflow-check` passed. |
| Release notes | `make release-notes-check` passed. |
| API/browser integration | `make integration` passed against local HTTP endpoints and Playwright browser smoke. |
| Frontend build/browser smoke | `make frontend-test` and `make frontend-e2e` passed. |
| Auditable domain archiving | `make check`, `make integration`, `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, and release/auditor static checks passed at local development evidence commit `ec67b3a87e2b530d82a280074c3505e62ada638f`; the React Library view archives and restores domains with read-only archived-domain guardrails, and `DELETE /domains/{domain_id}` plus `POST /domains/{domain_id}/restore` preserve corpus data while writing `domain.archived` and `domain.restored` journal evidence. |
| Auditable source removal | `make check`, `make integration`, `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, and release/auditor static checks passed at local development evidence commit `4e5f24b5b679d1cbc53e077721a4660467fbdc06`; the React Sources view removes a source after confirmation while existing documents remain available, and `DELETE /domains/{domain_id}/sources/{source_id}` writes `source.deleted` snapshot journal evidence. |
| Auditable source detail editing | `make check`, `make integration`, `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, and release/auditor static checks passed at local development evidence commit `cb22aec64c5954dcf5143f45035ddeb0e2478e28`; the React Sources view now edits source kind/name/URI inline, and `PATCH /domains/{domain_id}/sources/{source_id}` writes field-level `source.updated` journal evidence. |
| Auditable domain detail editing | `make check`, `make integration`, `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, and release/auditor static checks passed at local development evidence commit `330a72730069e65e82e0123000145e56839ac698`; the React Library view now edits active domain name/description in compact sections, and `PATCH /domains/{domain_id}` writes field-level `domain.updated` journal evidence. |
| Local UI compactness pass | `npm run check`, `npm run e2e`, and `npm run visual-audit` passed at local development evidence commit `027a73228da818872572d584c9c2001c7046702f`; in-browser inspection confirmed compact responsive module headers, tablet-width side-by-side Library panels, aligned toolbar toggles, hover/focus tooltips on ingestion/source/upload fields, and bounded dense panels on a 599x773 viewport. |
| Compact Overview and CI workflow guard | `make ci-workflow-check`, `make format-check`, `make lint`, `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make release-check`, `make audit-pack-check`, `make production-preflight`, `make auditor-static-check`, `make audit-manifest-check`, `make audit-bundle-check`, and `make check` passed at local development evidence commit `846df8e7dfe088a65eb9bb38ec036f7b5c7680f0`; in-browser inspection on a 599x773 viewport confirmed no global body scroll, Overview split into bounded status/workflow sections, workflow shortcuts readable in a scrollable panel, and 78 hover/focus tooltips available. The CI workflow contract is now checked offline for backend/frontend/docker/audit-evidence jobs, root Python script quality gates, required artifacts, and audit handoff export steps. |
| README onboarding guard | `make readme-check`, `make format-check`, `make lint`, `make release-check`, `make audit-pack-check`, `make production-preflight`, `make auditor-static-check`, `make audit-manifest-check`, `make audit-bundle-check`, and `make check` passed at local development evidence commit `66001426c102abba6108beaec4d96ff8cb9eb215`; README now has a first-minute path table for running, seeding, auditing, judging readiness, and agent-based development, and `scripts/check_readme_usability.py` preserves action pills, status badges, local commands, and Codex/Claude development guidance in the offline audit pack. |
| Compact query workspace and visual review evidence gate | `make format-check`, `make lint`, `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make visual-review-check`, `make promotion-template-check`, `make release-check`, `make audit-pack-check`, `make production-preflight`, `make auditor-static-check`, `make audit-manifest-check`, `make audit-bundle-check`, and `make check` passed at local development evidence commit `8a37334497667d7dfc6744f1a2e14ce161356c60`; the React query workspace now separates question and result sections on desktop, keeps mobile as a compact single-column flow, shortens module chrome, adds the missing job-detail close tooltip, and `docs/releases/evidence/visual-review-template.md` plus `scripts/check_visual_review.py` preserve human visual acceptance fields, screenshot hashes, hover/focus tooltip review, overflow review, findings, accepted risks, and sign-off in the offline audit manifest and bundle. |
| Document Library sectioning | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `e2fb0ff3eb155a40f58afb9399c84bdc090fa7ad`; the React Library module now separates domain controls from a compact document shelf with a sticky `Documents` heading, visible count, hover/focus tooltip, bounded list height, denser rows, and Playwright coverage for the section label and tooltip contract. |
| Compact two-column document Library layout | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `eccd51a56467ddd988601adcae21c2d79166d406`; the React Library module now keeps domain setup/edit controls and the document shelf in separate desktop panes, preserves mobile single-column behavior, verifies no horizontal overflow, keeps hover/focus tooltip coverage, and bounds panel/list scroll with Playwright layout checks. |
| Ignore hygiene guard tests | `make format-check`, `make lint`, and `make check` passed with 651 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `7809f8ba49e29c42e3c9872ac85580b1e024c079`; `backend/tests/test_ignore_hygiene_check.py` now exercises `scripts/check_ignore_hygiene.sh` in a temporary checkout, including failures when local volume ignores, Docker documentation exclusions, or documented local backup paths are removed. |
| Release workflow guard tests | `make format-check`, `make lint`, and `make check` passed with 655 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `ba8d7238bbf9700ed5af543bc0560c423cd18025`; `backend/tests/test_release_workflow_check.py` now exercises `scripts/check_release_workflow.sh` in a temporary checkout, including failures when Cosign signing is removed, a separate worker image is introduced, or release-process publishing documentation drops required Cosign evidence. |
| Docker topology guard tests | `make format-check`, `make lint`, and `make check` passed with 660 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `cf9fd6835101108e0f90716e468c70b32a614711`; `backend/tests/test_docker_topology_check.py` now exercises `scripts/check_docker_topology.sh` with a deterministic fake `docker compose config`, including failures when API/worker/migrate stop sharing one backend image, worker declares its own build, the backend target drifts from `backend-runtime`, or worker loses shared persistent state volumes. |
| CI workflow guard tests | `make format-check`, `make lint`, and `make check` passed with 665 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `321d27c6ff6f48aea441cbff59e77d8f6d59032a`; `backend/tests/test_ci_workflow_check.py` now exercises `scripts/check_ci_workflow.sh` in a temporary checkout, including failures when root Python formatting, visual audit manifest artifacts, README CI status guidance, or production readiness CI health evidence are removed. |
| Published release evidence guard tests | `make format-check`, `make lint`, and `make check` passed with 670 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `caffc2d15e97490cf845bbe15a0dac9564cf5ee5`; `backend/tests/test_published_release_evidence_check.py` now exercises `scripts/check_published_release_evidence.sh` in dry-run mode, including successful Cosign/tag verification command rendering plus failures for mutable or malformed versions, invalid digests, and non-GHCR image references. |
| Image metadata guard tests | `make format-check`, `make lint`, and `make check` passed with 675 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `4192838d89bb4e11b7109db17ce0f3b6ed36bd00`; `backend/tests/test_image_metadata_check.py` now exercises `scripts/check_image_metadata.sh`, including Dockerfile source-label failures, MIT license label failures, successful built-image label inspection through a fake Docker CLI, and missing built-image OCI label failures without requiring local image builds. |
| Image size guard tests | `make format-check`, `make lint`, and `make check` passed with 681 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `766098438af2703e95212279e1bb771e308e986d`; `backend/tests/test_image_size_check.py` now exercises `scripts/check_image_size.sh` with a temporary repo and fake Docker CLI, including current source docs, non-integer and undersized budgets, release-doc drift, successful built-image inspection, and built-image limit failures without building local images. |
| Backend runtime image guard tests | `make format-check`, `make lint`, and `make check` passed with 685 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `dc56d339da589f5e5edce86cc09c564259b332c4`; `backend/tests/test_backend_runtime_image_check.py` now exercises `scripts/check_backend_runtime_image.sh` with a fake Docker CLI, including api/worker/migrate sharing the same runtime image ID, missing backend role containers, worker image drift, and the no-env-file Compose mode without starting Docker. |
| Security policy guard tests | `make format-check`, `make lint`, and `make check` passed with 690 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `95556502d5f7b22991e7f0f3eb31d25af22be32e`; `backend/tests/test_security_policy_check.py` now exercises `scripts/check_security_policy.sh` with a temporary docs set, including the current security contract, reporting heading drift, paid-LLM-default drift, README policy link drift, and target security review auth-section drift. |
| Release notes guard tests | `make format-check`, `make lint`, and `make check` passed with 695 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `ad193c34f55deb7da278b90dace79d8519dfd809`; `backend/tests/test_release_notes_check.py` now exercises `scripts/check_release_notes.sh` with a temporary docs set, including current release docs, changelog `Unreleased` drift, visual-audit release-step drift, README changelog link drift, and operations release-process link drift. |
| Versioned release notes guard tests | `make format-check`, `make lint`, and `make check` passed with 700 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `b50e4abaa29e721bee1578a81da6a09c7ec7d7a0`; `backend/tests/test_versioned_release_notes_check.py` now exercises `scripts/check_versioned_release_notes.sh` with a temporary Git repo, including the current release-note contract, release index Cosign drift, stale backend coverage, stale pytest count, and missing local evidence commit references. |
| Operations runbook guard tests | `make format-check`, `make lint`, and `make check` passed with 705 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `a2a14ff5da2c9076c2bc70b2696a48ecaa0dbc39`; `backend/tests/test_operations_runbook_check.py` now exercises `scripts/check_operations_runbook.sh` with a temporary docs set, including the current backup/restore/rollback contract, rollback heading drift, backup ignore drift, API smoke audit-export drift, and backup/restore drill evidence drift. |
| Branding assets guard tests | `make format-check`, `make lint`, and `make check` passed with 711 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `3d278cf7a582958863d606f004d2984e1be98da9`; `backend/tests/test_branding_assets_check.py` now exercises `scripts/check_branding_assets.sh` with a temporary docs/frontend/workflow set, including the current brand contract, README project-card drift, CSS brand-token drift, SVG accessibility drift, favicon drift, and visual-audit artifact drift. |
| Dependency audit guard tests | `make format-check`, `make lint`, and `make check` passed with 714 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `4dea44713f4ed73c5cd6a8ccbbb58850ea8ef4a5`; `backend/tests/test_dependency_audit_check.py` now exercises `scripts/check_dependency_audit.sh` with fake Python and npm CLIs, including the expected `pip_audit` requirements invocation, frontend `npm audit --audit-level=high`, Python audit failure propagation, and Node audit failure propagation without network calls. |
| CI status guard tests | `make format-check`, `make lint`, and `make check` passed with 718 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `3ad1d75c0ef8311d7dfb7ba1119f88c05b83a4dc`; `backend/tests/test_ci_status_check.py` now exercises `scripts/check_ci_status.sh` with fake GitHub API responses, including successful CI jobs and required artifacts, missing CI runs, failed required jobs, and missing audit handoff artifacts without contacting GitHub. |
| Production preflight guard tests | `make format-check`, `make lint`, and `make check` passed with 723 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `9de0ea719edfbda4c7b4b69dfe9b5c40b1482e3b`; `backend/tests/test_production_preflight_check.py` now exercises `scripts/check_production_preflight.sh` with mocked local subchecks and real docs, including current contract success, pytest-count drift, release workflow preflight drift, process-tracker external blocker drift, and audit-pack subcheck failure propagation without contacting external services. |
| Audit pack guard tests | `make format-check`, `make lint`, and `make check` passed with 729 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `9a1f241edcac5e6de4b5c53affc2c1ce58c335cb`; `backend/tests/test_audit_pack_check.py` now exercises `scripts/check_audit_pack.sh` with a temporary docs/scripts/workflows set, including current contract success, local acceptance evidence drift, release-note publish blocker drift, CI audit manifest artifact drift, promotion-template visual review drift, and CI status artifact validation drift without contacting external services. |
| Compact workspace screens | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `4687b7b04bedb83398ead95e2c3a945070fbfb5e`; long query, eval, live progress, and audit surfaces now keep secondary evidence in bounded review sections, query plans open on demand, list-heavy ledgers use shorter internal scrolling, and mobile timelines collapse to one column while retaining hover/focus tooltips. |
| Document Sources sectioning | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `6e9ef1a801c8f3eb98a3167ead72305e8d3a659a`; the React Sources module now separates source creation from a compact registered-source shelf with a sticky `Registered sources` heading, visible registered count, hover/focus tooltip, bounded list height, denser rows, and Playwright coverage for the section label and tooltip contract. |
| Upload and text ingestion context | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `b89a62b9be8e59cee9e1418f40856ff2835b3d7d`; the React Upload and Text modules now show compact context panels for target domain, allowed file types or queued processing, source availability, and hover/focus tooltips before an operator queues ingestion work, with Playwright coverage for the visible context and tooltip contracts. |
| Eval scope context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `1a72ece667521bbee1c1badec8adcb3cb84ebd7e`; the React eval runner now shows compact context cards for dataset report scope, review/filter scope, and the no-paid-calls cost guard before operators run suites, with Playwright coverage for visible labels and hover/focus tooltip contracts. |
| Provider runtime context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `ceec406a4809113a7ba9bf731fcde29f0c8303eb`; the React provider admin module now shows compact context cards for the default local provider, Deep Agents harness, and paid-provider opt-in policy before operators preview runtime changes, with Playwright coverage for visible context and tooltip contracts. |
| Audit jobs context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `3e202faf7cc6a7d0313a72e13cf78564337a0215`; the React audit jobs module now shows compact context cards for visible filtered jobs, export scope, and the offline `make audit-export-check` verifier before operators refresh or export audit evidence, with Playwright coverage for visible context and tooltip contracts. |
| Admin users context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `68fe7301eec4257419ebfb6f9bb9a60bc8d76db9`; the React admin users module now shows compact context cards for active local users, per-domain viewer grants, and admin journal coverage before operators create users or change roles, status, grants, and passwords, with Playwright coverage for visible context and tooltip contracts. |
| Query run context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `a16a6ecd98099e5a5637c701ccdeec1f1fdee2c1`; the React query runner now shows compact context cards for target corpus, local Deep Agents runtime, and citation/budget audit evidence before operators run grounded questions, with Playwright coverage for visible context and tooltip contracts. |
| Live progress context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `dba946eeff77512ab3788771a1a7ad8f314cda3c`; the React live progress module now shows compact context cards for SSE connection, replay cursor, and persisted audit trail before operators connect to live processing updates, with Playwright coverage for visible context and tooltip contracts. |
| Audit events context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `84ac7483bac844e5ef41b89dec9a7b3259ed332c`; the React audit events module now shows compact context cards for journal rows, progress rows, and hash-chain integrity before operators inspect raw persisted ledgers, with Playwright coverage for visible context and tooltip contracts. |
| Audit progress context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `482f4fa7cc22001746cf79f2b6bc5d82eee7b6ad`; the React audit progress module now shows compact context cards for grouped jobs, persisted progress-event volume, and latest grouped state before operators inspect per-job progress timelines, with Playwright coverage for visible context and tooltip contracts. |
| Eval results and history context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `d739bee7a7a89f2210a9fe82572f6e91e3673825`; the React eval results and history modules now show compact context cards for result suite, metric/case counts, review scope, comparable runs, and trend suites before operators inspect eval metrics or regression history, with Playwright coverage for visible context and tooltip contracts. |
| Document library and source context cards | `make frontend-test`, `make frontend-e2e`, `make frontend-visual-audit`, `make visual-audit-check`, `make format-check`, and `make lint` passed at local development evidence commit `3836c2cf9f84d0e1db4d3438aba7245e3a88b04a`; the React document Library and Sources modules now show compact context cards for active domain, visible document/archive scope, registered source count, and local rebuild posture before operators edit domains, inspect documents, or queue source/index work, with Playwright coverage for visible context and tooltip contracts. |
| Document UI evidence traceability guard | `make readme-check`, `make auditor-evidence-matrix-check`, `make format-check`, `make lint`, `make audit-pack-check`, `make production-preflight`, `make audit-manifest-check`, and `make audit-bundle-check` passed at local development evidence commit `352b0523a58dbd860649b687aff6f87a2d01b8bb`; README, the auditor evidence matrix, and `scripts/check_auditor_evidence_matrix.sh` now preserve the compact document context-card contract for active domain, archive scope, registered source count, and local rebuild posture. |
| Auditor evidence matrix guard tests | `make check` passed with 643 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `779ef02d1a9dc3efeb6ee6eb04b7924aeb8b4f15`; `backend/tests/test_auditor_evidence_matrix_check.py` now exercises the objective-to-evidence matrix guard in a temporary checkout, including failures when compact document context-card evidence is removed from the matrix or README. |
| README usability guard tests | `make check` passed with 647 tests, 95.43% total coverage, and 90.78% branch coverage at local development evidence commit `8b5b1c8862b76743d39735bc6f6cfc22f53c3e3c`; `backend/tests/test_readme_usability_check.py` now exercises the README onboarding guard directly, including failures for missing current-status heading, missing local acceptance action, and incorrect status/workflow ordering. |
| Production readiness test-count guard | `make production-preflight`, `make versioned-release-notes-check`, `make release-notes-check`, `make format-check`, `make lint`, `make audit-pack-check`, `make audit-manifest-check`, and `make audit-bundle-check` passed at local development evidence commit `ff66bab16ba1fa6d4f3b558dbf1af95300a2467c`; production readiness and versioned release notes now record the 729-test backend suite, and `scripts/check_production_preflight.sh` guards the current pytest count plus unique rows for backend quality, security policy, README usability, and auditor evidence matrix. |
| Expired session recovery | `npm --prefix frontend run check`, `npm --prefix frontend run e2e`, `make format-check`, and `make check` passed at local development evidence commit `e2645246d1e9815c79b3ef37ab785c7439ec4b34`; Playwright covers expired stored admin tokens from both provider loading and workspace refresh, including local storage cleanup and visible reconnect feedback. |
| Calibration evidence gate | `make eval-calibration-gate`, `make audit-manifest-check`, `make release-check`, `make auditor-static-check`, and `make check` passed at local development evidence commit `7f6580764e0537fa6b8c4bc6f69fee6e570d2bd7`; the versioned calibration evidence is now checked offline for PASS status, required targets, minimum records/cases, required metric gates, HTTPS source URLs, and local path safety. |
| Calibration trend evidence gate | `make eval-calibration-trend-gate`, targeted gate tests, `make audit-manifest-check`, `make release-check`, `make auditor-static-check`, and `make check` passed at local development evidence commit `62d1d56a3632c302a523e9d569c5d21401977ebc`; the versioned calibration trend evidence is now checked offline for PASS status, sample growth, required targets, metric regression tolerance, HTTPS source URLs, and local path safety. |
| Local doctor `.env` security checks | `make doctor`, targeted local doctor tests, `make format-check`, and `make check` passed at local development evidence commit `7265292b32eaf22c5fb43e7edddacb9aabee8fba`; the local doctor now validates the active `.env` when present for production placeholders, wildcard CORS outside development, invalid provider profiles, paid-provider opt-in alignment, and short secrets. |
| Standalone `.env` security gate | `make env-security-check`, targeted env-security tests, `make audit-manifest-check`, `make release-check`, and `make check` passed at local development evidence commit `c4df48b4f32991b1920ff4c4f0ddb7a4adec4de3`; operators can now validate active `.env` production placeholders, wildcard CORS outside development, invalid providers, paid-provider opt-in drift, and short secrets without starting services. |
| Visual audit evidence gate | `make visual-audit-check`, targeted visual-audit tests, `make audit-manifest-check`, `make release-check`, `make auditor-static-check`, and `make check` passed at local development evidence commit `84baf4e639481c9d59e0e26ed765212979768fe8`; the local visual-audit manifest now has an offline verifier for expected desktop/mobile screenshot records, PNG existence, byte sizes, SHA-256 hashes, and viewport dimensions. |
| Promotion template gate alignment | `make audit-pack-check`, `make release-check`, and `make auditor-static-check` passed at local development evidence commit `93e05a72e5ca7de044aa0f3251c2e4fdb49e116d`; the human promotion evidence template now requires `make env-security-check` and `make visual-audit-check` output alongside security and visual review sign-off fields. |
| Standalone promotion template gate | `make promotion-template-check`, targeted promotion-template tests, `make audit-pack-check`, `make release-check`, `make audit-manifest-check`, and `make check` passed at local development evidence commit `6430f43c7b2ecb367a787889feaa5550a44228b8`; the production promotion evidence template is now validated as a first-class local gate for required sections, machine evidence, release provenance, visual/security review, backup/restore rehearsal, rollback, and decision fields. |
| Backup/restore drill evidence gate | `make backup-restore-drill-check`, targeted backup/restore drill tests, `make operations-runbook-check`, `make audit-bundle-check`, `make release-check`, `make audit-pack-check`, `make audit-manifest-check`, and `make check` passed at local development evidence commit `d56906e70e9efd135ab2133d6506812b06849346`; the backup/restore rehearsal template now records backup artifacts, disposable restore commands, health checks, audit-export validation, and promotion impact as a first-class local evidence artifact. |
| Target security review evidence gate | `make target-security-review-check`, targeted target-security-review tests, `make security-policy-check`, `make promotion-template-check`, `make operations-runbook-check`, `make audit-bundle-check`, `make release-check`, `make audit-pack-check`, `make audit-manifest-check`, `make auditor-static-check`, and `make check` passed at local development evidence commit `e0610b92d2432aa62219b507d1de4c77f93b92c9`; the target-environment security review template now records auth, secrets, provider keys, CORS, exposed ports, data handling, release provenance, rollback, accepted risks, and promotion impact as a first-class local evidence artifact. |
| Calibration scope decision evidence gate | `make calibration-scope-decision-check`, targeted calibration-scope-decision tests, `make promotion-template-check`, `make operations-runbook-check`, `make audit-handoff-report-check`, `make audit-bundle-check`, `make release-check`, `make audit-pack-check`, `make audit-manifest-check`, `make auditor-static-check`, and `make check` passed at local development evidence commit `2b8e2c21c6fde6b0dd1a7046ec3a0c10e063a8a9`; the calibration scope decision template now records versioned calibration evidence, bounded pilot scope acceptance, broader trend evidence, accepted risks, required follow-up issues, and promotion impact as a first-class local evidence artifact. |
| Offline audit bundle calibration evidence | `make audit-bundle-check`, `make audit-pack-check`, `make release-notes-check`, `make versioned-release-notes-check`, `make format-check`, and `make auditor-static-check` passed at local development evidence commit `c47f95d231a88355ceeeb300982d6fc001646970`; the offline auditor bundle now includes the versioned calibration evidence and calibration trend evidence alongside the manifest, handoff report, release docs, promotion template, and workflows. |
| Docker stack smoke | `make docker-smoke` passed and confirmed API, worker, and migrate share one backend image ID. |
| Branding/UI audit | `make frontend-visual-audit` generated desktop/mobile screenshots plus hash metadata in `frontend/visual-audit/manifest.json`, and `docs/branding.md` records the RetOS palette, mark, responsive console contract, and latest visual checks. |
| CI | `https://github.com/nfernandezsanz/retOS/actions/runs/28341158419` completed success for backend, frontend, docker, and audit-evidence. |
| Audit handoff manifest | The CI run uploads `retos-backend-coverage-94e48fa63360a4bf706994fc0b055613dd503143` from the backend test job, downloads it in `audit-evidence`, and uploads `retos-audit-manifest-94e48fa63360a4bf706994fc0b055613dd503143` as an in-run snapshot. Post-run `make audit-manifest` records commit state, coverage source, final CI jobs, critical file hashes, visual audit artifacts, required gates, and remaining external promotion evidence. |
| Audit handoff report | `make audit-handoff-report-check`, `make audit-bundle-check`, `make auditor-static-check`, and `make check` passed at local development evidence commit `b04d0aff3244663411762b97e77e107c40165c66`; the CI run uploads `retos-audit-handoff-94e48fa63360a4bf706994fc0b055613dd503143` as a human-readable Markdown companion generated from the same manifest snapshot, and the local report now includes an evidence-status table plus a promotion decision checklist for separating local evidence from external promotion decisions. |
| Root Python script quality gate | `make format-check`, `make lint`, `make release-workflow-check`, `make release-check`, `make audit-pack-check`, `make production-preflight`, and `make check` passed at local development evidence commit `7388d7281bd5160eb5277a8c8d6304634ae45c47`; root Python audit/release scripts are now included in Black and Ruff locally plus CI/release workflows. |
| Local audit bundle | `make auditor-handoff-check` exports `evals/reports/retos-audit-handoff.tar.gz` and `evals/reports/retos-audit-handoff.tar.gz.sha256` for offline reviewer transfer. |
| Real-dataset calibration | `docs/releases/evidence/2026.06.28-alpha.1-calibration.md` records a 200-record public dataset sample with 40 evaluated cases per target over SQuAD, HotpotQA, HotpotQA-agent, and NQ-Open adapter samples. |
| Real-dataset trend | `docs/releases/evidence/2026.06.28-alpha.1-calibration-trend.md` records a PASS comparison from 100-record/30-case baseline to 200-record/40-case candidate with zero allowed regression across SQuAD, HotpotQA, HotpotQA-agent, and NQ-Open adapter samples. |

Publishing evidence still required before promoting this draft to a final GitHub release:

| Gate | Evidence |
| --- | --- |
| GHCR publishing | Pending until `.github/workflows/release.yml` runs for the release tag. |
| Image digests | Pending until GHCR publish completes. |
| SBOM/provenance | Pending until GHCR publish completes. |
| Cosign signatures and signature verification | Pending until GHCR publish completes; final evidence must include `make release-evidence-check` output for both published digests. |

## Known Limitations

- Product maturity is pre-alpha; phases remain in progress in `planning/04-process-tracker.md`.
- Larger real-dataset trend calibration now has a 100-record/30-case to
  200-record/40-case PASS comparison. Broader public slices beyond 200 records and 40
  cases per target still need trend evidence before release promotion.
- Multi-hop planning is deterministic, auditable, and budgeted; broader learned routing
  heuristics remain planned beyond the alpha.
- Eval reruns depend on persisted runnable payloads.

## Rollback

Rollback is image-tag based:

```bash
export RETOS_IMAGE_TAG=<previous-tag>
docker compose up -d migrate
docker compose up -d api worker web
make docker-smoke
```

Restore Postgres and storage backups if a migration or data-shape change must be undone.
Keep failed release logs, image digests, and eval reports with the incident notes.
